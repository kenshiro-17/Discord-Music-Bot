# Discord Configuration (Required)
DISCORD_TOKEN=your_bot_token_here
DISCORD_CLIENT_ID=your_client_id_here

# Sentry Configuration (Optional - for error tracking)
SENTRY_DSN=your_sentry_dsn

# Application Configuration
NODE_ENV=development
LOG_LEVEL=info
DEFAULT_VOLUME=50
MAX_QUEUE_SIZE=100
INACTIVITY_TIMEOUT=300
MAX_FILE_SIZE=25
HEALTH_CHECK_PORT=8080

# =============================================================================
# YouTube Configuration (Optional but recommended)
# =============================================================================
# The bot uses PoToken generation to bypass YouTube's bot detection.
# No Google account is required for basic functionality.
#
# If you experience issues, try these options:

# Option 1: YouTube Cookies (Optional)
# ------------------------------------
# Export cookies from a YouTube session using browser extension
# This can help with some restrictions
# Format: Netscape format (tab-separated)
YOUTUBE_COOKIES=

# Option 2: Proxy (For cloud-hosted bots)
# ---------------------------------------
# If running on cloud servers (Railway, Heroku, etc.) with blocked IPs,
# use a residential proxy to route YouTube requests
# Format: http://user:pass@host:port or socks5://user:pass@host:port
YOUTUBE_PROXY=

# =============================================================================
# TIPS FOR BEST RESULTS
# =============================================================================
# 1. Self-hosting on home network (residential IP) works best
# 2. Cloud servers often have blocked IPs - use a residential proxy
# 3. If playback fails, check logs for specific error messages
